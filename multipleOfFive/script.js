function fiveMultiple(n)
{
    let i = 5;
    while(i<=n)
    {
        document.write(i + " ");
        i += 5;
    }
}

let num = Number(prompt("Enter the higer range"));
let multipleoffive = fiveMultiple(num);
document.write(totalEvenNumbers);